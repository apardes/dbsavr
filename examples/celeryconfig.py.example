"""
Example Celery configuration for dbsavr scheduled backups.
This file is automatically generated by the 'dbsavr setup_celery_schedule' command.
"""

from celery.schedules import crontab

broker_url = 'redis://localhost:6379/0'
result_backend = 'redis://localhost:6379/0'

task_serializer = 'json'
result_serializer = 'json'
accept_content = ['json']
timezone = 'UTC'
enable_utc = True

beat_scheduler = 'celery.beat.PersistentScheduler'
beat_schedule = {
    'backup-myapp_db-0': {
        'task': 'backup_database',
        'schedule': crontab(
            minute='0',
            hour='2',
            day_of_month='*',
            month_of_year='*',
            day_of_week='*'
        ),
        'args': ['myapp_db']
    },
    'backup-analytics_db-1': {
        'task': 'backup_database',
        'schedule': crontab(
            minute='0',
            hour='3',
            day_of_month='*',
            month_of_year='*',
            day_of_week='0'
        ),
        'args': ['analytics_db']
    }
}